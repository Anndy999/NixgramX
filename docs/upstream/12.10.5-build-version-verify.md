# 12.10.5 BUILD/VERSION verify + upstream-base close-out prep

> Issue **#145**. Verify-only — L2 already applied `APP_VERSION_*`. Do **not** flip `docs/upstream-base.json` `base` in this task. Do **not** bump `NIXGRAMX_VERSION_*` / `APP_PACKAGE`.

_Verified: 2026-09-26 (Asia/Shanghai CST). Branch `upstream-task/12.10.5/145-build-version` from `upstream-sync/12.10.5` tip `9a623e42`._

## Three-way result

| Field | Official OLD `c8480176` | Official NEW `dc780e81` | NixgramX on sync tip | Action |
|---|---|---|---|---|
| `APP_VERSION_CODE` | 7099 | **7105** | **7105** | Already correct (L2) |
| `APP_VERSION_NAME` | 12.10.4 | **12.10.5** | **12.10.5** | Already correct (L2) |
| `NIXGRAMX_VERSION_NAME` | — | — | **12.10.4** | Must stay |
| `NIXGRAMX_VERSION_CODE` | — | — | **1353** | Must stay |
| `APP_PACKAGE` | `org.telegram.messenger` | same | **`app.nixgramx.android`** | Must stay |

## `docs/upstream-base.json` (prep only — Owner close-out)

Current on `upstream-sync/12.10.5` (L2 prepare):

- `base`: still **12.10.4 / 7099 / `c84801762fd5…`**
- `pending` + `prepared_target`: already **12.10.5 / 7105 / `dc780e81ed12…`**
- `synced_commits`: includes through `c8480176…` (12.10.4); does **not** yet include `dc780e81…`

### Owner close-out checklist (after all 12.10.5 task PRs land)

1. Flip `base` → `{ "version": "12.10.5", "build": "7105", "commit": "dc780e81ed1261c369c27870e8e0999a1eb0b600" }`
2. Append `dc780e81ed1261c369c27870e8e0999a1eb0b600` to `synced_commits`
3. Set `pending` → `null`
4. Refresh `prepared_target` / `prepared_base` / `refreshed_nix_base` per `docs/UPSTREAM_SYNC.md` adaptation gate
5. Do **not** bump `NIXGRAMX_VERSION_*` as part of upstream close-out (channel release is separate)

This issue / PR does **not** perform steps 1–4.

## QV pin

`Tools/stability/test_upstream_12103_incremental.py` updated to expect:

- `APP_VERSION_CODE=7105` / `APP_VERSION_NAME=12.10.5`
- `NIXGRAMX_VERSION_NAME=12.10.4` (fork distribution; code stays unconstrained integer)
- `APP_PACKAGE=app.nixgramx.android`
- In-progress sync shape: `base.commit=c8480176…`, `pending.commit=dc780e81…`

## Out of scope

Round Video / media / theme / DB Java; merges to `main`/`beta`; releases; device gate (unless Owner asks).
